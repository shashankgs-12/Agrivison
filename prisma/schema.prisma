generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Farm {
  id        Int      @id @default(autoincrement())
  name      String
  location  String
  area      Float
  createdAt DateTime @default(now())
}